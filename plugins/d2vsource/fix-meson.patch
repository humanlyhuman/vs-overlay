--- a/meson.build
+++ b/meson.build
@@ -27,13 +27,7 @@ deps = [libavcodec_dep, libavutil_dep, libavformat_dep]
-incdir = include_directories(
-    run_command(
-        find_program('python', 'python3'),
-        '-c',
-        'import vapoursynth as vs; print(vs.get_include())',
-        check: true,
-    ).stdout().strip(),
-    'src/core'
-)
+incdir = include_directories('src/core')
